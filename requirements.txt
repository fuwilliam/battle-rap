# Ingestion + transform pipeline (runs in GitHub Actions)
spotapi
# spotapi imports these storage backends at load time but doesn't declare them
pymongo
redis
websockets
# MotherDuck / DuckDB
duckdb
dbt-duckdb
# data handling
pandas
python-dotenv
